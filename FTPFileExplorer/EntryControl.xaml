<UserControl x:Class="FTPFileExplorer.EntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FTPFileExplorer"
             mc:Ignorable="d" 
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Medium"
            TextElement.FontSize="14"
            FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"       
            d:DesignHeight="36" d:DesignWidth="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Image Height="32" Width="32" x:Name="Img" Grid.Column="0" HorizontalAlignment="Left"></Image>
        <TextBlock x:Name="FileName" Margin="44,0,0,0" Grid.Column="0" VerticalAlignment="Center" />
        <TextBlock x:Name="FileSize" Margin="12,0,0,0" Grid.Column="1" VerticalAlignment="Center" />
        <TextBlock x:Name="Date" Margin="12,0" Grid.Column="2" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
